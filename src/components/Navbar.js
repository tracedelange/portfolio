import React from 'react'

const Navbar = () => {
    return (
        <div id='navigation'>

            <button>Home</button>
            <button>Projects</button>
            <button>Contact</button>
            
        </div>
    )
}

export default Navbar;
